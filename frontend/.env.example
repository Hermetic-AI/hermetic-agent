# OpenAgent Frontend Environment (template)

# Backend base URL.  Empty string uses the Vite dev proxy at /api.
# In production builds, set to the absolute backend URL, e.g.
#   VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
