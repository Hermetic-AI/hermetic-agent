script-shell=C:\\Windows\\System32\\cmd.exe
# pnpm 11 默认会弹"node_modules 要重装, 是否继续?"提示.
# 在 Windows ↔ WSL 混合开发环境 (lockfile 路径是 /mnt/c/..., 但 pnpm 是 native Windows)
# 下, 这个检查每次都会触发, 很烦. 关掉它, pnpm 会自动 purge + reinstall 不再问.
confirmModulesPurge=false
