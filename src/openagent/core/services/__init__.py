"""Service layer for the core/ domain.

Each module here provides a thin *Service class that encapsulates a single
responsibility on top of the AgentBridge. Naming follows the harness
`*Service` suffix rule.
"""
