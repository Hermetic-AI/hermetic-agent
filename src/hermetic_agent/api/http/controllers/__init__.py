"""Per-resource controllers (Sanic Blueprints).

Each controller owns one resource and exposes a Blueprint of HTTP routes
that delegate to the service layer. Naming follows the harness
`*_controller.py` convention.
"""
