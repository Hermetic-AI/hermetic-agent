"""hermetic_agent.api.shared — API 层共享类型/常量.

预留: 未来跨 controller 复用的:
  - 错误码常量 (e.g. ROUTING_FAILED, TIMEOUT …)
  - 通用 Pydantic 基类 (BaseResponse, ErrorResponse 统一)
  - 跨子包的 shared 配置

当前空, 等后续重构 (scenario 错误码 / chat 错误码统一收编) 时填充.
"""
